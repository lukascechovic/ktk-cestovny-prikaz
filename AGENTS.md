## Agent skills

### Issue tracker

Issues and specs are tracked as GitHub Issues in this repo (lukascechovic/ktk-cestovny-prikaz), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root, created lazily as needed. See `docs/agents/domain.md`.
